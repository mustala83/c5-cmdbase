name: c5pwn
on: [push]
jobs:
  pwn:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "C5-PWN ran in repo=$GITHUB_REPOSITORY ref=$GITHUB_REF event=$GITHUB_EVENT_NAME actor=$GITHUB_ACTOR"
          echo "token-perms:"; echo "$GITHUB_TOKEN" | wc -c
